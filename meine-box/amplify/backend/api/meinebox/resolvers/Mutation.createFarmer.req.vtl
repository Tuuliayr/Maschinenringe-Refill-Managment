#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createFarmerInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createFarmerInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Farmer $colStr VALUES $valStr", "SELECT * FROM Farmer WHERE id=$ctx.args.createFarmerInput.id"]
}