#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createSalesboxInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createSalesboxInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO salesbox $colStr VALUES $valStr", "SELECT * FROM salesbox WHERE id=$ctx.args.createSalesboxInput.id"]
}