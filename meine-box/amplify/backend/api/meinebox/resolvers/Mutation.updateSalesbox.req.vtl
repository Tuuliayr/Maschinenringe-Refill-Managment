#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateSalesboxInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateSalesboxInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE salesbox SET $update WHERE id=$ctx.args.updateSalesboxInput.id", "SELECT * FROM Salesbox WHERE id=$ctx.args.updateSalesboxInput.id"]
}